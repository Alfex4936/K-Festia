type Query {
    festivals(
        month: String,
        latitude: Float,
        longitude: Float,
        page: Int,
        size: Int
    ): FestivalPage

    searchFestivals(
        query: String!,
        page: Int,
        size: Int
    ): FestivalPage
}
